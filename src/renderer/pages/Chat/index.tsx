// P0b: route the Chat page through the Mastra + AI SDK UI panel.
// The legacy ChatPanel (bloom-response-v1 + Timeline/reducer) stays on disk until P5 cleanup.
export { ChatPanelMastra as ChatPanel } from './ChatPanelMastra'
